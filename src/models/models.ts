import {
  User,
  IUser,
  validate as validateUser,
  Address,
  AddressType,
} from "./userModel";

import { Shoe, validate as validateShoe } from "./shoeModel";

export { User, validateUser, IUser, Address, AddressType, Shoe, validateShoe };
